spring:
  datasource:
    # ✅ Docker network içinde postgres servis adına bağlan
    url: jdbc:postgresql://postgres:5432/product_service
    username: admin
    password: secret123
    driver-class-name: org.postgresql.Driver
  elasticsearch:
    uris: http://elasticsearch:9200
  data:
    redis:
      host: redis
      port: 6379
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
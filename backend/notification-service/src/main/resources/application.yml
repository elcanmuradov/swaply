spring:
  application:
    name: notification-service
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      lettuce:
        pool:
          max-active: 10
          max-idle: 10
          min-idle: 1
  mail:
    username: elcanmuradov25@gmail.com
    password: cmpa pbdb ijar dryw
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
    host: smtp.gmail.com
    port: 587

server:
  port: 8080
